# Sample Data

이 폴더의 `news_articles.csv`는 baseline 계산을 검증하기 위해 만든 합성 데이터입니다.

## Columns

- `article_id`
- `published_at`
- `symbol`
- `headline`
- `summary`

## Usage

```bash
python -m src.run_baseline
```

